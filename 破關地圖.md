# 🗺️ 破關地圖 — 操作手冊
給未來的自己看的工作流程，不需要學後端，不需要資料庫。

📁 檔案結構
exampleSite/
├── data/
│   └── places.json          ← 地點資料庫（主要文件）
├── static/
│   ├── images/places/       ← 地點照片存放區
│   └── tools/
│       └── place-helper.html ← 地點管理與新增工具
├── layouts/adventure-map/
│   └── list.html            ← 地圖頁面 Template
├── static/js/
│   └── adventure-map.js     ← 地圖互動邏輯
└── static/css/
    └── adventure-map.css    ← 地圖樣式

✅ 地點管理（新增、編輯、刪除）
第一步｜啟動 Hugo 開發伺服器
cd c:\Users\user\Documents\GitHub\exampleSite
.\hugo.exe server -D --port 1313 --bind 0.0.0.0

第二步｜開啟地點輔助工具
瀏覽器（Chrome 或 Edge）打開：
<http://localhost:1313/tools/place-helper.html>
⚠️ 必須用 Chrome 或 Edge，Firefox 不支援直接寫檔功能。

### 第三步｜連接檔案（每次重開工具需執行一次）

1. 點擊「選擇 places.json 檔案」 → 選擇 `data/places.json`
2. 點擊「選擇照片儲存資料夾」 → 選擇 `static/images/places/`
3. 連接後按鈕會變為 ✓ 已連接。

### 第四步｜模式切換

工具上方有兩個標籤：

- 「新增地點」：填寫表單新增全新地點。
- 「管理與編輯」：搜尋現有地點，進行【編輯】或【刪除】。

### 第五步｜編輯現有地點

1. 切換到「管理與編輯」標籤。
2. 在搜尋框輸入名稱找到地點。
3. 點擊「編輯」，資料會自動填回表單。
4. 修改完成後，點擊下方的「更新地點：[名稱]」。

### 第六步｜新增地點流程

1. 填寫名稱、選擇類別。
2. 在「搜尋地址」輸入後按 Enter 定位。
3. **季節感知**：勾選適合前往的季節。
4. **精選評分**：為地點打上 1-5 星評分。
5. 如有照片，直接加（自動處理目錄）。
6. 點擊「生成並寫入 places.json」。

### 第七步｜多次破關紀錄 (Visit History)

地圖現在支援記錄同一個地點多次造訪的心情：

1. 將狀態切換為「已破關」。
2. 在「新增造訪紀錄」區域填寫日期與筆記。
3. 點擊「+ 加入紀錄」，舊的紀錄會保留成列表。
4. 地圖側邊欄會顯示完整的歷史訪客清單。

### 第八步｜確認 & 推上 GitHub

```bash
git add data/places.json static/images/places/
git commit -m "feat: 更新破關地圖資料"
git push
```

🌟 關於「類別」

現在支援更多類別，顏色與圖示會自動對應：

- 🍜 美食 / ☕ 咖啡 / 🛍️ 購物 / 🏨 住宿
- 🌿 風景 / 🏮 老店 / 🚶 散步 / ⚡ 任務

🔗 分享地點

在地圖頁面上點擊任何地點，側邊欄下方會有一個「分享」按鈕。
點擊後會複製專屬網址，別人打開該網址會直接定位到該地點。

最後更新：2026-03-15
